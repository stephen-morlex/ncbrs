<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>NCBRS.Client.Harness</RootNamespace>
  </PropertyGroup>

  <!-- A narrated end-to-end walk of the offline client-core: enrol, unlock,
       register (including block exhaustion → provisional), build a signed
       upload and a transfer file, verify both, and settle a simulated centre
       response. Not a test — a runnable demonstration and living documentation
       of how FacilityClient is driven. No device tooling: plain console. -->
  <ItemGroup>
    <ProjectReference Include="../NCBRS.Client.Core/NCBRS.Client.Core.csproj" />
  </ItemGroup>

</Project>
